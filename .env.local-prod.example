# AI Product Factory - Local Production-Parity Environment Variables
# ==================================================================
# Copy this file to .env.local-prod and customize as needed
#
# Usage:
#   cp .env.local-prod.example .env.local-prod
#   docker compose -f docker-compose.local-prod.yml --env-file .env.local-prod up -d
#
# NOTE: These are TEST values safe for local development only.
#       Never use these values in production!

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n_test_password
POSTGRES_DB=n8n

# ===========================================
# n8n Configuration
# ===========================================
# Encryption key must be exactly 32 characters
N8N_ENCRYPTION_KEY=test-encryption-key-32chars!!!

# Timezone for workflow scheduling
TIMEZONE=Europe/Berlin

# ===========================================
# S3/SeaweedFS Configuration
# ===========================================
# These are test credentials for local SeaweedFS
# SeaweedFS in simple mode doesn't require auth
S3_ACCESS_KEY=admin
S3_SECRET_KEY=admin123
S3_BUCKET=product-factory-artifacts

# ===========================================
# Vector Database (Qdrant)
# ===========================================
# Leave empty for no authentication (local testing)
QDRANT_API_KEY=

# ===========================================
# Knowledge Graph (Graphiti)
# ===========================================
# Required for Graphiti but not used for health checks
# Use a placeholder or real key if testing AI features
OPENAI_API_KEY=sk-test-key-not-used-for-healthcheck

# ===========================================
# AI Factory Configuration
# ===========================================
FACTORY_MAX_ITERATIONS=5
FACTORY_SCORE_THRESHOLD=90
FACTORY_BATCH_SIZE=3
FACTORY_CONFIRMATION_TIMEOUT=3600

# ===========================================
# Model Configuration (OpenRouter)
# ===========================================
MODEL_ARCHITECT=anthropic/claude-sonnet-3.5
MODEL_CRITIC=openai/gpt-4o
MODEL_REFINER=anthropic/claude-sonnet-3.5
MODEL_CONTEXT=google/gemini-1.5-pro
MODEL_RESEARCH=perplexity/sonar-pro

# ===========================================
# Authentication (Optional for local testing)
# ===========================================
# 32+ character secret for session encryption
AUTH_SECRET=test-auth-secret-32-characters!!

# Google OAuth (leave empty to disable)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Email domain restriction (comma-separated, leave empty to allow all)
ALLOWED_EMAIL_DOMAINS=

# ===========================================
# Local Domains (auto-resolve to 127.0.0.1)
# ===========================================
# These are used by Traefik for routing
# No configuration needed - .localhost domains work automatically
#
# Access points:
#   - http://n8n.localhost        - n8n workflow engine
#   - http://dashboard.localhost  - Frontend dashboard
#   - http://s3.localhost         - SeaweedFS S3 storage
#   - http://localhost:8080       - Traefik dashboard
